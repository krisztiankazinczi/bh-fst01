const s = 'Use the force, Luke';

// concat

// endswith
// startswith

// includes

// indexof
// lastindexof

// padend
// padstart

// repeat

// slice
// split

// trim
// trimend
// trimstart
console.log(s.endsWith('uke'));
console.log(s.startsWith('use')); // case sensitive!

console.log(s.indexOf('Luke'));
console.log('sir, yer, sir!'.lastIndexOf('sir'));

// padEnd: pad end until long enough
// padStart

console.log('js is cool!'.repeat(3));

// slice: return section of string
// split: split into array using delimiter
// substring (substring vs slice$)

// trim, trimEnd, trimStart
