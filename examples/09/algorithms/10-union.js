let positives = [1, 4, 9, 8, ];
let negatives = [-4, -5];
let union = [];
let i = 0, j = 0, k = 0;

while (i < positives.length) {
    union[i] = positives[i];
    i = i + 1;
}

while(j < negatives.length) {
    while(k < union.length) {
        if (union[k] != negatives[j]) {
            
        }
    }
}

console.log(negatives);
console.log(positives);