export function sum(...numbers) {
    let sum = 0
    numbers.forEach(num => sum += num)
    return sum
}

export function product(...numbers) {
    let product = 0
    numbers.forEach(num => product *= product)
    return sum
}